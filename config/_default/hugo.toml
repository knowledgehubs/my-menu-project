# ⚠️ هام جداً: الرابط ينتهي بـ / لضمان عمل المسارات الفرعية
baseURL = "https://knowledgehubs.github.io/my-menu-project/"
languageCode = "en-us"
title = "Digital Menu Engine"

# لتصحيح الروابط النسبية عند النشر
canonifyURLs = true

[params]
  # 🕹️ لوحة التحكم: غيّر الاسم هنا لتبديل شكل الموقع بالكامل
  # القيم المتاحة: "burger_joint" أو "oriental_grill" (أو أي مجلد تنشئه في data)
  active_skin = "burger_joint"

[server]
  disableFastRender = true

[markup.goldmark.renderer]
  unsafe = true
